# ─────────────────────────────────────────────────────────────────────────────
# .env.local — NEVER commit this file
# ─────────────────────────────────────────────────────────────────────────────

# Neon PostgreSQL — pooled (app code)
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require

# Neon PostgreSQL — unpooled direct (drizzle-kit migrations only)
DATABASE_URL_UNPOOLED=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require&pgbouncer=false

# App
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# IP hashing salt
IP_SALT=change-me-to-a-random-secret

# Auth.js (next-auth v5)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-random-secret-here
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Resend — magic link emails + reminder/edit emails
RESEND_API_KEY=re_your_key_here
RESEND_FROM_EMAIL=Memoriza <noreply@yourdomain.com>

# Cloudinary — cover image uploads
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Upstash Redis — rate limiting on /api/create (optional, degrades gracefully)
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token-here

# Vercel Cron protection
CRON_SECRET=change-me-to-a-random-secret
